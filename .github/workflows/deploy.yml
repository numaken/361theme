name: Deploy theme to server

on:
  push:
    branches: [ main ]
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      ref:
        description: 'Git ref (tag/branch) to deploy'
        required: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.ref || github.ref_name }}

      - name: Prepare artifact
        run: |
          set -e
          mkdir -p dist
          VERSION="${GITHUB_REF_NAME:-manual}"
          echo "Preparing artifact for $VERSION"
          rsync -a --delete --exclude='.git' --exclude='dist' ./ ./dist/361theme/
          ls -lah dist/361theme | head -n 50

      - name: Debug environment variables
        run: |
          echo "=== Environment Variable Check ==="
          echo "DEPLOY_HOST exists: ${{ secrets.DEPLOY_HOST != '' }}"
          echo "FTP_SERVER exists: ${{ secrets.FTP_SERVER != '' }}"
          echo "FTP_USERNAME exists: ${{ secrets.FTP_USERNAME != '' }}"
          echo "FTP_REMOTE_DIR: ${{ secrets.FTP_REMOTE_DIR }}"
          
      - name: FTP debug (print PWD and list candidates)
        run: |
          set +e
          echo "== PWD at login root =="
          curl -v --ftp-ssl --ssl-reqd -u "${{ secrets.FTP_USERNAME }}:${{ secrets.FTP_PASSWORD }}" "ftp://${{ secrets.FTP_SERVER }}/" -Q "PWD" || true
          echo "\n== List root =="
          curl -sS --ftp-ssl --ssl-reqd -u "${{ secrets.FTP_USERNAME }}:${{ secrets.FTP_PASSWORD }}" "ftp://${{ secrets.FTP_SERVER }}/" -l || true
          echo "\n== List /public_html =="
          curl -sS --ftp-ssl --ssl-reqd -u "${{ secrets.FTP_USERNAME }}:${{ secrets.FTP_PASSWORD }}" "ftp://${{ secrets.FTP_SERVER }}/public_html/" -l || true
          echo "\n== List /public_html/kyoto =="
          curl -sS --ftp-ssl --ssl-reqd -u "${{ secrets.FTP_USERNAME }}:${{ secrets.FTP_PASSWORD }}" "ftp://${{ secrets.FTP_SERVER }}/public_html/kyoto/" -l || true
          echo "\n== List /wp-content/themes =="
          curl -sS --ftp-ssl --ssl-reqd -u "${{ secrets.FTP_USERNAME }}:${{ secrets.FTP_PASSWORD }}" "ftp://${{ secrets.FTP_SERVER }}/wp-content/themes/" -l || true
          echo "\n== Current server-dir from secret =="
          echo "${{ secrets.FTP_REMOTE_DIR }}"

      - name: Deploy via rsync over SSH
        uses: burnett01/rsync-deployments@6.0.0
        if: ${{ secrets.DEPLOY_HOST != '' }}
        with:
          switches: -avz --delete
          path: dist/361theme/
          remote_path: ${{ secrets.DEPLOY_PATH }}
          remote_host: ${{ secrets.DEPLOY_HOST }}
          remote_user: ${{ secrets.DEPLOY_USER }}
          remote_key: ${{ secrets.DEPLOY_SSH_KEY }}

      - name: Deploy via FTPS
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        if: ${{ secrets.FTP_SERVER != '' }}
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          protocol: ftps
          port: ${{ secrets.FTP_PORT || 21 }}
          local-dir: dist/361theme/
          server-dir: ${{ secrets.FTP_REMOTE_DIR }}

      - name: Purge opcode cache (optional)
        if: ${{ env.PURGE_CMD != '' }}
        run: echo "Add a remote cache purge step if needed."
